CREATE TABLE `table_name` (
                                       `ID` INT(11) NOT NULL AUTO_INCREMENT,
                                       `NAME` VARCHAR(65536) NOT NULL,
                                       PRIMARY KEY(ID)
);